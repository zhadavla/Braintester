#ifndef MAIN_HEADER_H
#define MAIN_HEADER_H

enum MENU_STATES{
  MATH_SEL, LED_SEL, SCORE_SEL
};

enum GAME_STATES{
  SELECTION, MATH_GAME
};

#include <Esplora.h>
#include <TFT.h>
#include <SPI.h>
#include <time.h>

#endif 
